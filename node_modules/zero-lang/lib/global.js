'use strict';
const undefStr = 'undefined';

let result = {};

if (typeof window !== undefStr) {
  result = window;
} else if (typeof global !== undefStr) {
  result = global;
} else if (typeof self !== undefStr) {
  result = self;
}

module.exports = result;

