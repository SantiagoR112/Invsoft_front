const assert = chai.assert;
const lang = require('../lib');

mocha.setup('bdd');

describe('lang', () => {
  it('exists', () => {
    assert.typeOf(lang, 'object');
  });
});

require('./array.spec');
require('./global.spec');
require('./number.spec');
require('./object.spec');
require('./string.spec');
require('./type.spec');

mocha.run();

